mkdir dirname
rmdir dirname
cd dirname
cd ..

git config --global user.name "username"
git config --global user.email "useremail"
gti config user.name "username"
git config user.email "useremail"

git pull origin main

git status
git add Hello.html
git commit -m "new file"
git push origin main
git branch
git branch dir01
git checkout dir01
git branch -d dir01
git push origin --delete dir01
git branch -m 변경전 변경후

git remote
git remote -v
git remote add origin http://~
git remote set-url origin http://~  // remote url 변경
git remote remove origin